#/usr/bin/perl -w
###########################################################
#2019-4-8
#解决的问题:根据fastq文件信息,提取title行和sequence行
#技巧:利用特殊变量$.记录当前读取的行号
###########################################################
my ($file) =@ARGV;
open IN,"$file" or die "$!";
while (<IN>) {
	#print "$.\n";
	print "$_" if ($. % 4 == 1 or $. % 4 == 2);
}
